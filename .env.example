# Example environment variables for deploying the Driver Applicants portal

# Base (client) branding
VITE_COMPANY_NAME=Your Company
VITE_COMPANY_LOGO_URL=https://example.com/logo.png
VITE_PRIMARY_COLOR=#3b82f6
VITE_SUPPORT_EMAIL=support@example.com

# Google service account credentials (base64-encoded JSON). Server-only.
# Create a service account in Google Cloud, give it Drive and Sheets permissions,
# download the JSON, then base64-encode it (no newlines) and paste the output here.
GOOGLE_SERVICE_ACCOUNT_BASE64=
GOOGLE_SHEETS_ID=
GOOGLE_DRIVE_FOLDER_ID=

# A simple server-side API key to protect the /api/google endpoint (server-only)
SERVER_API_KEY=

# Optional: third-party email provider (SendGrid, Mailgun) credentials for notifications
EMAIL_PROVIDER_API_KEY=
